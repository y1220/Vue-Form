export const userNameKey = Symbol('userName');
export const updateUserNameKey = Symbol('updateUserName');
// These symbols can be used as keys to access or define properties in objects.
// Unlike strings or numbers, symbols are guaranteed to be unique, meaning
